# 标注数据构建步骤

- 检查tk_data中启用的字种是否有重复，若有则需去重
- 检查km_cluster中启用的字种是否有重复，若有则需去重
- 检查、设置config.get_select_conds函数中的参数
    * 允许从多张数据表中选择数据
    * 设置flag：1，纳入选择的字图；0，排除的字图
- 初始化任务init_task
- 同时运行多个脚本执行任务
    * 完成后，有些字种可能由于kmeans聚类失效而退出，需针对这些字种，重新进行kmeans聚类，然后再次运行脚本

# 单个字种构建的业务逻辑

